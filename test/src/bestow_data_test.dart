// ignore_for_file: prefer_const_constructors
import 'package:bestow_data/bestow_data.dart';
import 'package:test/test.dart';

void main() {
  group('BestowData', () {
    test('can be instantiated', () {
      expect(BestowData(), isNotNull);
    });
  });
}
